import { Home } from "@/views/home";
//pushing again to get the PR link as i have merged previously but now nothing is available to show so i made this change

export default function HomeRoot() {
  return <Home />;
}
